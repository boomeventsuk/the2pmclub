

# Lighten Hero Image Overlay

## Change

**File**: `src/components/NewHero.tsx`

Reduce the gradient overlay opacity so the crowd/confetti photo is more visible:

- Change `from-background via-background/30` to `from-background/70 via-background/15` — this lightens the bottom fade and middle section significantly while still keeping text readable.

